# Windows Platform

Windows POS-first runtime scaffold for keyboard-speed billing.

Initial scope:
- barcode-first POS
- hold/resume bills
- split payment
- printer/scanner support bridge

Current baseline delivered:
- runtime package scaffold (`package.json`)
- POS module map in `src/pos-baseline.md`
