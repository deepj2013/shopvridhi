# Mobile Platform

Android-first runtime scaffold for operator workflows.

Initial scope:
- login
- dashboard KPIs
- inventory
- order inbox

Current baseline delivered:
- runtime package scaffold (`package.json`)
- module map for auth/dashboard/inventory/orders/offline queue in `src/mobile-baseline.md`
