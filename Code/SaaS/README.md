# SaaS Code Area

SaaS implementation is organized as:

- `public-website/web` for website and frontend app
- `working/api` for backend services
- `working/mobile` for mobile track
- `working/windows` for windows/POS track
